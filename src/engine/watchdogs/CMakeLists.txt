if (${IS_CROSS} STREQUAL "TRUE")
	set (CMAKE_SHARED_LINKER_FLAGS "-Wl,--no-as-needed")
	qi_create_bin(pineapple "watchdog.cpp")
	qi_use_lib(pineapple luatables lua)
else()
	set (CMAKE_SHARED_LINKER_FLAGS "-Wl,--no-as-needed")
	qi_create_bin(pineapple "watchdog.cpp")
	qi_use_lib(pineapple luatables lua)
endif()
cmake_minimum_required(VERSION 2.8)
project(NAO-engine)
find_package(qibuild)

# Create a executable named n_a_o-engine
# with the source file: main.cpp
#qi_create_bin(NAO-engine "main.cpp")

# Add a simple test:
#qi_create_test(test_n_a_o-engine "test.cpp")
